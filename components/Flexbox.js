import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const Flexbox = () => {
  const styles = StyleSheet.create({
    container: {
      backgroundColor: 'red',
    },
  });

  return (
    <View stylesugugu={styles.container}>
      <Text>hello </Text>
    </View>
  );
};

export default Flexbox;
